"""
dashboard/charts.py
────────────────────
Phase 3 — Reusable Plotly chart factory functions.

Functions (to be implemented):
  - rolling_avg_line(df, unit_id, sensor_id) → go.Figure
  - degradation_rank_bar(df)                 → go.Figure
  - rul_table(df)                            → st.dataframe
  - out_of_range_gauge(pct)                  → go.Figure
  - health_trend_sparkline(df)               → go.Figure

TODO (Phase 3): implement all chart functions.
"""
